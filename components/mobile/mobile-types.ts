export type MobileView = 'home' | 'hourly' | 'daily' | 'radar' | 'settings';

export interface MobileLabels {
  back: string;
  daily: string;
  home: string;
  hourly: string;
  locate: string;
  radar: string;
  rain: string;
  search: string;
  settings: string;
  temperature: string;
}
